# Step 2: Create Output Directory

Create a dedicated directory for FastQC output reports:

```bash
mkdir -p output
```

This creates the `output/` directory where all FastQC HTML reports and zip files will be saved.
